## Example of using wasm-bindgen

### Building

```bash
wasm-pack build --target web
```

or just use [./build.sh](./build.sh)

### Running

Serve the statics and open the page in
the browser. Check the browser console for
the output.
